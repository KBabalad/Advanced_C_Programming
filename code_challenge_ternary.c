/*

 Hi There in this code we are going to learn the use of ternary operators to decide the given number is an odd or even
 step 1: Tell the user to input a number.
 step 2: Save the user input using the scanf.
 step 3: Divide the number by 2 if we receive the remainder to be zero, the number is odd else even:
 step 4: make the above decision using the ternary operator and print the statement
*/
#include<stdio.h>
#include<stdint.h>


/*int main()
{
 int a;
 printf("Please enter a number:");
 scanf("%d",&a);
 ((a%2)==0)?printf("Number is Even\n"):printf("Number is odd");
 return 0;
}*/
